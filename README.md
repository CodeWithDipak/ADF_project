# ADF_project
Everything about the Azure Data Factory. Import data from on Prem, SQLDB, git repo and create the medallion architecture to segregate the data. Created integration runtimes, linked services, pipelines, dataflows, triggers and much more.
